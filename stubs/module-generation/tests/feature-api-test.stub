<?php

declare(strict_types=1);

namespace Tests\Feature\{{ moduleNamespace }};

{{ userRoleImportLine }}
use App\Models\{{ modelClass }};
use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

final class {{ pagePascalName }}ApiTest extends TestCase
{
    public function test_guests_access_behavior_for_{{ routeTestName }}_api_index(): void
    {
        $testResponse = $this->getJson('{{ apiRouteUri }}');

        {{ apiGuestAssertion }}
    }

{{ forbiddenRoleTest }}
    public function test_authenticated_users_can_list_{{ routeTestName }}_api_results(): void
    {
        {{ apiAuthRequestLine }}

        $testResponse->assertOk();

        {{ modelClass }}::query()->create(['name' => 'Alpha']);

        $testResponse = {{ apiAuthFollowUpLine }};

        $testResponse->assertOk();
        $testResponse->assertJsonStructure([
            'data' => [
                [
                    'id',
                    'name',
                    'created_at',
                    'updated_at',
                ],
            ],
        ]);
    }
}
