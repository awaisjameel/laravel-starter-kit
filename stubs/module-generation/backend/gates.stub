<?php

declare(strict_types=1);

use App\Enums\UserRole;
use App\Models\User;
use Illuminate\Support\Facades\Gate;

Gate::define('{{ abilityName }}', static fn (User $user): bool => in_array($user->role, [{{ allowedRoleCases }}], true));
